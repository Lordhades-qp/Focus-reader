name: focus_reader
description: Une application Flutter pour lire et traduire des fichiers texte à voix haute.
version: 0.2.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^3.6.3
  file_picker: ^5.2.6
  translator: ^0.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
